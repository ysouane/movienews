import React from "react"
import { render } from "@testing-library/react"
import SelectDisplay from "./SelectDisplay"

describe("SelectDisplay component", () => {
  it("Should return without error", () => {
    render(<SelectDisplay />)
  })
})
